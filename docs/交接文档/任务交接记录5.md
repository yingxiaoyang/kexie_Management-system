# 科研项目材料管理平台任务交接记录

## 2026-08-06：对话 5 - 业务功能开发

## 已完成

- 阅读推进计划、对话 3/4 交接、数据表设计、导入模板设计和接口契约。
- 保留并吸收工作区中已有的任务模板、账号角色和负责人提交页改动。
- 建立前端统一请求层，统一处理：
  - API 基础地址
  - JWT 请求头
  - JSON 与表单上传
  - 登录过期
  - 网络失败
  - 文件下载
- 实现真实登录、退出、当前用户恢复、首次改密页面和角色路由拦截。
- 实现管理员项目列表、新增、编辑和筛选。
- 实现标准工作簿两阶段导入：
  - 上传 `.xlsx`
  - 五张业务表预检查
  - 重复与关联校验
  - 错误清单下载
  - `create_only` / `upsert` 事务入库
- 实现人员库多字段搜索、新增、编辑和人员参与项目查看。
- 实现管理员账号列表、负责人/管理员账号创建、启停和密码重置。
- 实现材料任务列表、创建、草稿编辑、发布、关闭、重新开放、材料类别和任务模板上传。
- 实现负责人任务范围计算、模板下载、文件上传和最新提交状态展示。
- 上传时校验负责人项目归属、任务范围、任务状态、格式、单文件大小和累计大小。
- 实现管理员提交列表、文件查看与下载、审核通过和填写原因退回。
- 实现负责人查看退回原因并重新提交。
- 将管理员总览、负责人工作台、我的项目和项目通讯录切换为真实数据。
- 为核心表格和表单补齐加载中、空数据、字段校验和接口失败提示。

## 后端新增文件

- `server\src\routes\accounts.js`
- `server\src\routes\imports.js`
- `server\src\routes\people.js`
- `server\src\routes\projects.js`
- `server\src\routes\submissions.js`
- `server\src\utils\query.js`

## 前端新增文件

- `code\kexie_vue\src\services\http.js`
- `code\kexie_vue\src\stores\auth.js`
- `code\kexie_vue\src\views\LoginView.vue`
- `code\kexie_vue\src\views\ChangePasswordView.vue`

## 主要修改文件

- `server\src\app.js`
- `server\src\routes\materialTasks.js`
- `server\src\routes\uploads.js`
- `server\package.json`
- `code\kexie_vue\src\router\index.js`
- `code\kexie_vue\src\style.css`
- 管理员项目、人员、账号、任务、审核、总览页面
- 负责人工作台、项目、材料提交、通讯录页面
- `docs\前端运行说明.md`
- `docs\接口契约\后端接口契约初稿.md`

## 验证结果

后端：

```powershell
cd D:\kexiexitong\server
npm.cmd run check
```

结果：通过。

前端：

```powershell
cd D:\kexiexitong\code\kexie_vue
npm.cmd run build
```

结果：通过。仍有 Element Plus 主包较大的既有构建警告，不阻塞第一版试用。

真实流程已验证：

1. 标准工作簿预检查通过。
2. 项目、人员、参与关系、检查记录和报销记录事务导入成功。
3. 管理员创建负责人账号和材料任务。
4. 管理员上传任务模板。
5. 负责人上传材料。
6. 管理员填写原因退回。
7. 负责人重新提交。
8. 管理员审核通过。

浏览器人工验收已覆盖：

- 登录和按角色跳转
- 管理员总览
- 项目管理
- 人员库
- 材料任务
- 负责人工作台
- 负责人材料提交
- 页面中文显示
- 浏览器控制台无错误

## 本机演示数据

本机数据库已加入明确标注的演示项目、演示人员、演示任务、任务模板以及一组“退回后重交并通过”的提交记录，方便直接查看页面。

本机另有演示导入文件：

```text
D:\kexiexitong\storage\demo-import-20260806.xlsx
```

`storage` 属于本地运行数据，不提交 GitHub。演示账号密码也不写入仓库文档。

## 运行方式

后端：

```powershell
cd D:\kexiexitong\server
npm.cmd run dev
```

前端：

```powershell
cd D:\kexiexitong\code\kexie_vue
npm.cmd run dev
```

访问：

```text
http://127.0.0.1:5173/login
```

## 尚未包含

- 归档模板配置和整理包导出仍属于对话 7。
- 归档模板、整理包导出和参与规则页面仍保留示例数据。
- 项目参与关系的后台手工维护页面尚未实现；当前通过标准工作簿导入参与关系。
- 页面深度视觉优化和手机端人工验收属于对话 6。
- 当前 Excel 解析依赖用于管理员上传的标准工作簿；正式公网部署前应继续做依赖安全审计和恶意文件隔离。
