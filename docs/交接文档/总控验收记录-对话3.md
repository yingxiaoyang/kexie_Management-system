# 对话 3 总控验收记录

验收日期：2026-08-05

## 验收结论

对话 3“现有前端梳理与基础配置”已完成，可以进入后端服务和数据库模型阶段。

已复验：

- 前端生产构建通过。
- 入口页、负责人端、管理员端主要路由可访问。
- 中文源码和网页显示正常；PowerShell 默认编码导致的控制台乱码不代表网页乱码。
- 主要页面标题正常，浏览器控制台未发现错误。
- 管理员“新增项目”等基础弹窗可以打开。
- 前端环境变量和后端接口基础地址已经集中配置。
- 验收开始时 Git 工作区干净，前端提交已经位于 `main` 并同步到远程仓库。

## 当前不是缺陷、但后续必须完成

- 当前业务数据来自 `src\data\mock.js`。
- 搜索、筛选、保存、上传、审核、下载和导出大多只是页面交互示意，尚未接真实接口。
- 尚无登录、退出、密码管理和角色路由拦截。
- 尚无统一请求层、接口错误提示和真实文件上传流程。
- 移动端和不同屏幕宽度需要在对话 6 进行人工视觉验收。
- 构建提示主包较大，第一版不阻塞，业务稳定后再优化。

## 总控调整

- 对话 4 增加账号角色确认、登录方案、接口返回规范、文件上传约束和跨域配置。
- 对话 5 增加统一前端请求层、模拟数据替换、权限拦截、表单校验和异常状态。
- 对话 6 增加桌面端与手机端人工验收。
- 明确共享文件和并行工作规则，真正并行编码时使用独立 Git worktree。

## 已确认的总控决定

- 第一版取消“只读查看人员”账号，只保留项目负责人和管理员。
- 允许多个管理员账号，第一版管理员权限保持一致。
- 项目负责人使用管理员生成的初始密码，首次登录修改；忘记密码由管理员重置。
- 默认支持常用文档、图片和 ZIP；单文件不超过 50MB，单个项目在一个材料任务下累计不超过 500MB。
- 管理员发布材料任务时可以选择是否提供模板，并允许上传多个模板文件供负责人下载。

## 后续任务工作目录

真实仓库为 `D:\kexiexitong`。Codex 中已经保存同名 D 盘项目，后续新任务应从该项目创建，不再使用搬迁前的 C 盘旧工作区。

本次总控调整及前端页面壳修改目前尚未提交 Git，四项账号与上传规则已经确认，后续可将这些变更作为一次总控需求更新提交。
