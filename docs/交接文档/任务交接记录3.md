# 科研项目材料管理平台任务交接记录

## 2026-08-05：对话 3 - 现有前端梳理与基础配置

### 已完成

- 阅读 `docs` 下的推进计划和交接文件，确认本对话只负责现有前端梳理与基础配置。
- 保留已有 `code\kexie_vue` Vue 3 + Vite + Element Plus 项目，没有重新创建前端。
- 补全 Vue Router 路由，形成项目负责人端和管理员端两个入口。
- 新增负责人端页面结构：工作台、我的项目、材料提交、项目通讯录。
- 新增管理员端页面结构：管理总览、项目管理、人员库、账号管理、材料任务、材料审核、归档模板、整理包导出、参与规则。
- 新增前端接口地址配置，统一从 `VITE_API_BASE_URL` 读取后端 API 地址。
- 替换 `App.vue` 默认按钮页面，避免继续停留在默认示例和中文显示误判。
- 清理未再使用的 Vue/Vite 默认示例组件和素材，使 `src\views` 成为页面实现的明确入口。
- 完善根目录 `.gitignore`，本地环境变量、依赖、构建产物、缓存、日志和数据库运行数据不会进入 GitHub。
- 新增前端运行说明。
- 已执行 `npm.cmd run build`，构建通过。
- 已启动本地开发服务并访问验证，`http://127.0.0.1:5173/` 返回正常。

### 新增或修改文件

- `code\kexie_vue\src\main.js`
- `code\kexie_vue\src\App.vue`
- `code\kexie_vue\src\style.css`
- `code\kexie_vue\src\router\index.js`
- `code\kexie_vue\src\config\api.js`
- `code\kexie_vue\src\data\mock.js`
- `code\kexie_vue\src\views\HomeView.vue`
- `code\kexie_vue\src\views\owner\OwnerLayout.vue`
- `code\kexie_vue\src\views\owner\OwnerDashboard.vue`
- `code\kexie_vue\src\views\owner\OwnerProjects.vue`
- `code\kexie_vue\src\views\owner\OwnerSubmissions.vue`
- `code\kexie_vue\src\views\owner\OwnerContacts.vue`
- `code\kexie_vue\src\views\admin\AdminLayout.vue`
- `code\kexie_vue\src\views\admin\AdminDashboard.vue`
- `code\kexie_vue\src\views\admin\AdminProjects.vue`
- `code\kexie_vue\src\views\admin\AdminPeople.vue`
- `code\kexie_vue\src\views\admin\AdminAccounts.vue`
- `code\kexie_vue\src\views\admin\AdminMaterialTasks.vue`
- `code\kexie_vue\src\views\admin\AdminReviews.vue`
- `code\kexie_vue\src\views\admin\AdminArchiveTemplates.vue`
- `code\kexie_vue\src\views\admin\AdminExports.vue`
- `code\kexie_vue\src\views\admin\AdminRules.vue`
- `code\kexie_vue\.env.example`
- `code\kexie_vue\.env.development`
- `code\kexie_vue\README.md`
- `docs\前端运行说明.md`
- `docs\交接文档\任务交接记录3.md`
- `.gitignore`

已移除不再使用的默认示例文件：

- `code\kexie_vue\src\components\HelloWorld.vue`
- `code\kexie_vue\src\assets\hero.png`
- `code\kexie_vue\src\assets\vite.svg`
- `code\kexie_vue\src\assets\vue.svg`
- `code\kexie_vue\public\icons.svg`

### 如何运行和验证

进入前端目录：

```bash
cd D:\kexiexitong\code\kexie_vue
```

启动：

```bash
npm.cmd run dev
```

访问：

- `http://127.0.0.1:5173/`
- `http://127.0.0.1:5173/owner/dashboard`
- `http://127.0.0.1:5173/admin/dashboard`

构建检查：

```bash
npm.cmd run build
```

### 后续对话需要知道

- 当前页面使用 `src\data\mock.js` 中的示例数据，不包含真实业务接口。
- 后端接口地址统一从 `src\config\api.js` 读取，默认 `http://127.0.0.1:3000/api`。
- 本机使用的 `.env.development` 已被 Git 忽略；仓库只提交不含敏感信息的 `.env.example`。
- 管理员端已经按 Element Plus 表格、筛选、表单、弹窗方式搭好页面壳，后续业务开发可直接在这些页面上接接口。
- 负责人端已具备门户式工作台入口，后续可以继续美化和接入材料上传、退回重交流程。

### 不包含

- Docker 安装或 MySQL 配置。
- 复杂后端业务接口。
- 真实登录鉴权和权限拦截。
